---
/** Ancho máximo y márgenes laterales del sitio. */
const { class: clase = "", ...resto } = Astro.props;
---
<div class={`mx-auto w-full max-w-[1200px] px-[18px] md:px-6 ${clase}`} {...resto}>
  <slot />
</div>
